---
- name: Create MTV Storage and Network Maps
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create maps for source-target pair
      ansible.builtin.import_role:
        name: infra.openshift_virtualization_migration.mtv_maps
...
